set1 = {45,67,3,24,67,}
print(sum(set1)) # we can sum all number in sets
# we can sort a set by converting it into a list
unique_set = list(set1)
unique_set.sort()
print(unique_set)
# we can find highest, second highest by using index in it 
print(unique_set[-1])